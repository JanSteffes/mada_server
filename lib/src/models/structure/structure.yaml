Structure:
  properties:
    name:
      type: string
      description: 'name of this structure'
    description:
      type: string
      description: '(Usage-)description for this structure'
    version:
      type: string
      description: 'versioncode of this structure'
    category:
      type: string
      description: 'to which category this structure belongs'
    # contents:
    #   $ref: 'content.yaml#/Content'
    #   description: 'contentfields of this structure' 